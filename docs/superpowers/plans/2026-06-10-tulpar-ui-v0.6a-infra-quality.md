# Tulpar UI v0.6a — Infra & Quality Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.6 Bölüm B — CI/CD, paket altyapısı, tokens bölünmesi, lint kuralları, dokümantasyon ve LICENSE; shell implementasyonunun (v0.6b planı) ön koşulları dahil.

**Architecture:** Mevcut pnpm workspace'i pnpm 9 + catalogs'a taşınır; core'un exports/vite entry'leri dizin taramasından üretilir (shell paketi aynı deseni devralacak); tokens semantic sözleşmesi bileşen-başına dosyalara bölünür; ESLint'e primitive-token ve `_internal` sınır kuralları eklenir; GitHub Actions'a PR CI + changesets release akışı kurulur.

**Tech Stack:** pnpm 9 (catalogs), GitHub Actions, changesets, ESLint flat config, tsx/node scripts.

**Sıralama kısıtı:** Task 1–5 shell implementasyonundan (v0.6b) ÖNCE bitmeli. Task 6–10 paralel/sonra yapılabilir.

**Spec:** `docs/superpowers/specs/2026-06-10-tulpar-ui-v0.6-shell-design.md` (Bölüm 12)

---

## Task 1: pnpm 9 + dependency catalogs

**Files:**
- Modify: `package.json` (root)
- Modify: `pnpm-workspace.yaml`
- Modify: `packages/*/package.json`, `apps/*/package.json` (catalog'lanan dep'ler)

- [ ] **Step 1: pnpm 9'a geç**

`package.json` (root) içinde:

```json
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22.22.3",
    "pnpm": ">=9.0.0"
  }
```

Sonra: `corepack enable; corepack use pnpm@9.15.9` (corepack packageManager alanını günceller — alanın `pnpm@9.15.9` kaldığını doğrula).

- [ ] **Step 2: Hangi dep'ler birden çok pakette — tara**

Run: `pnpm -r exec node -e "const p=require('./package.json'); for (const k of ['dependencies','devDependencies']) for (const [n,v] of Object.entries(p[k]||{})) console.log(n+'\t'+v+'\t'+p.name)"` (veya Grep ile `"lit"|"vite"|"vitest"|"tsx"|"vite-plugin-dts"|"@open-wc/testing"` ara)

Beklenen aday liste (2+ pakette geçenler): `lit`, `vite`, `vite-plugin-dts`, `vitest`, `tsx`.

- [ ] **Step 3: Catalog tanımla**

`pnpm-workspace.yaml`:

```yaml
packages:
  - "packages/*"
  - "apps/*"

catalog:
  lit: ^3.1.0
  vite: ^5.1.0
  vite-plugin-dts: ^3.7.0
  vitest: ^1.3.0
  tsx: ^4.7.0
```

(Step 2 taramasında 2+ pakette çıkan başka dep varsa catalog'a ekle — sürüm olarak en güncel kullanılan range'i al.)

- [ ] **Step 4: package.json'larda sürümleri `catalog:` ile değiştir**

Catalog'daki her dep için, geçtiği her workspace package.json'da:

```json
"lit": "catalog:",
"vite": "catalog:",
"vite-plugin-dts": "catalog:",
"vitest": "catalog:",
"tsx": "catalog:"
```

- [ ] **Step 5: Lockfile'ı yeniden üret ve doğrula**

Run: `pnpm install`
Expected: lockfileVersion '9.0' ile pnpm-lock.yaml yeniden yazılır, hata yok.

Run: `pnpm build`
Expected: tüm paketler topolojik sırayla başarıyla derlenir.

Run: `pnpm test`
Expected: tüm testler geçer (tokens vitest, core wtr, angular/vue vitest).

- [ ] **Step 6: Commit**

```bash
git add package.json pnpm-workspace.yaml pnpm-lock.yaml packages apps
git commit -m "chore: migrate to pnpm 9 with dependency catalogs"
```

---

## Task 2: Core exports + vite entries dizin taramasından

**Files:**
- Create: `packages/core/build/sync-exports.ts`
- Modify: `packages/core/vite.config.ts`
- Modify: `packages/core/package.json`

- [ ] **Step 1: Entry tarama yardımcısını vite.config'e yaz**

`packages/core/vite.config.ts` tamamını şununla değiştir:

```ts
import { readdirSync, existsSync } from "node:fs";
import { resolve } from "node:path";
import { defineConfig } from "vite";
import dts from "vite-plugin-dts";

/** src/<dir>/index.ts olan her dizin bir entry'dir; _internal hariç. */
export function scanEntries(srcDir: string): Record<string, string> {
  const entries: Record<string, string> = { index: resolve(srcDir, "index.ts") };
  for (const d of readdirSync(srcDir, { withFileTypes: true })) {
    if (!d.isDirectory() || d.name.startsWith("_")) continue;
    const idx = resolve(srcDir, d.name, "index.ts");
    if (existsSync(idx)) entries[`${d.name}/index`] = idx;
  }
  return entries;
}

export default defineConfig({
  build: {
    lib: {
      entry: scanEntries(resolve(__dirname, "src")),
      formats: ["es"],
    },
    outDir: "dist",
    sourcemap: true,
    rollupOptions: {
      external: ["lit", "lit/decorators.js"],
    },
  },
  plugins: [
    dts({
      entryRoot: "src",
      outDir: "dist",
      tsconfigPath: "./tsconfig.json",
    }),
  ],
});
```

- [ ] **Step 2: exports senkron script'ini yaz**

Create `packages/core/build/sync-exports.ts`:

```ts
import { readdirSync, existsSync, readFileSync, writeFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const pkgPath = resolve(__dirname, "../package.json");
const srcDir = resolve(__dirname, "../src");

const exportsMap: Record<string, unknown> = {
  ".": { types: "./dist/index.d.ts", import: "./dist/index.js" },
};
for (const d of readdirSync(srcDir, { withFileTypes: true })) {
  if (!d.isDirectory() || d.name.startsWith("_")) continue;
  if (!existsSync(resolve(srcDir, d.name, "index.ts"))) continue;
  exportsMap[`./${d.name}`] = {
    types: `./dist/${d.name}/index.d.ts`,
    import: `./dist/${d.name}/index.js`,
  };
}

const pkg = JSON.parse(readFileSync(pkgPath, "utf8"));
const next = JSON.stringify(exportsMap);
const current = JSON.stringify(pkg.exports);

if (process.argv.includes("--check")) {
  if (next !== current) {
    console.error("exports drift: run `pnpm --filter @tulpar-ui/core run sync-exports`");
    process.exit(1);
  }
  console.log("exports in sync");
} else {
  pkg.exports = exportsMap;
  writeFileSync(pkgPath, JSON.stringify(pkg, null, 2) + "\n");
  console.log("exports updated");
}
```

- [ ] **Step 3: Script'leri bağla**

`packages/core/package.json` scripts'e ekle (tsx devDependency olarak da eklenir, `"tsx": "catalog:"`):

```json
"sync-exports": "tsx build/sync-exports.ts",
"sync-exports:check": "tsx build/sync-exports.ts --check",
"prebuild": "tsx build/sync-exports.ts"
```

- [ ] **Step 4: Çalıştır ve drift'i doğrula**

Run: `pnpm --filter @tulpar-ui/core run sync-exports`
Expected: `exports updated` — package.json exports alanı 6 entry içerir (`.`, `./button`, `./button-group`, `./text-input`, `./textarea`, `./number-input`). Mevcut elle yazılmış haliyle diff'i incele; subpath kırıkları (varsa) bu noktada görünür.

Run: `pnpm --filter @tulpar-ui/core run build`
Expected: dist'te her entry için `index.js` + `index.d.ts`.

Run: `pnpm --filter @tulpar-ui/core run sync-exports:check`
Expected: `exports in sync`, exit 0.

- [ ] **Step 5: Tüketici doğrulaması**

Run: `pnpm build; pnpm test`
Expected: angular/vue wrapper'ları (`@tulpar-ui/core/text-input` import eden) derlenir ve testleri geçer.

- [ ] **Step 6: Commit**

```bash
git add packages/core
git commit -m "build(core): generate exports and vite entries from src directory scan"
```

---

## Task 3: `_internal` katmanlama kararı + lint sınırı

**Files:**
- Create: `docs/architecture/internal-layering.md`
- Modify: `eslint.config.js`

- [ ] **Step 1: Karar dokümanını yaz**

Create `docs/architecture/internal-layering.md`:

```markdown
# `_internal` Layering Decision (v0.6, kalıcı)

**Karar tarihi:** v0.6a — "for now" çözümü değil, bağlayıcı kural.

## Kurallar

1. Her paketin `src/_internal/` dizini **paket-özelidir**: hiçbir public
   entry'den re-export edilmez, `exports` map'ine girmez, dokümante edilmez.
2. Bir paket başka bir paketin `_internal`'ından **asla import edemez**
   (ESLint ile zorlanır).
3. İki paket aynı iç koda ihtiyaç duyarsa:
   - Kod ≤ ~50 satırsa **kopyalanır** (her kopya kendi paketinde test edilir).
   - Daha büyükse `@tulpar-ui/base` adlı yayınlanan ama dokümante edilmeyen
     bir paket oluşturulur ve kod oraya taşınır. Bu paket ilk gerçek ikinci
     tüketici ortaya çıktığında kurulur — önden kurulmaz (YAGNI).
4. Bilinen ilk aday: shell'in rail flyout pozisyonlama mantığı ↔ gelecekteki
   public Tooltip. Tooltip yapılırken kural 3 uygulanır.

## Gerekçe

- Premature paylaşım paketi = sürüm/yayın yükü, API donması.
- Kontrolsüz cross-import = packlenmiş dist'lerde kırık path'ler, gizli API
  sözleşmeleri. İkisi de reddedildi; eşik kuralı (50 satır) ikisinin ortası.
```

- [ ] **Step 2: ESLint sınır kuralını ekle**

`eslint.config.js` dizisine (mevcut TS bloğundan sonra) ekle:

```js
  {
    files: ["packages/*/src/**/*.ts", "apps/**/*.ts"],
    rules: {
      "no-restricted-imports": [
        "error",
        {
          patterns: [
            {
              group: ["@tulpar-ui/*/_internal*", "@tulpar-ui/*/dist/_internal*"],
              message:
                "_internal is package-private. See docs/architecture/internal-layering.md",
            },
          ],
        },
      ],
    },
  },
```

- [ ] **Step 3: Lint'i çalıştır**

Run: `pnpm lint`
Expected: PASS (mevcut kodda cross-package `_internal` importu yok; varsa bu task'ta düzeltilir — import eden kod kural 3'e göre kopyalama/taşıma ile çözülür).

- [ ] **Step 4: Commit**

```bash
git add docs/architecture/internal-layering.md eslint.config.js
git commit -m "docs(architecture): permanent _internal layering rule + eslint boundary"
```

---

## Task 4: tokens `semantic/types.ts` bileşen-başına bölünmesi

**Files:**
- Create: `packages/tokens/src/semantic/color.ts`, `packages/tokens/src/semantic/button.ts`, `packages/tokens/src/semantic/input.ts`, `packages/tokens/src/semantic/common.ts`
- Modify: `packages/tokens/src/semantic/types.ts`

- [ ] **Step 1: Mevcut interface'leri yeni dosyalara taşı**

Mevcut `types.ts` içeriği bloklar halinde taşınır (içerik birebir, sadece dosya değişir):

`semantic/color.ts` → `ColorTokens`, `VariantColorTokens` (types.ts'teki 6–42. satır bloğu, export'lar aynı isimle)

`semantic/button.ts` → `ButtonSizeTokens`, `ButtonTokens` (44–65. satır bloğu)

`semantic/input.ts` → `InputSizeTokens`, `InputTokens` (67–120. satır bloğu)

`semantic/common.ts`:

```ts
export interface FontTokens {
  family: {
    display: string;
    ui: string;
    mono: string;
  };
}

export interface TransitionTokens {
  default: string;
}
```

- [ ] **Step 2: types.ts'i kompozisyon + re-export'a indir**

`packages/tokens/src/semantic/types.ts` yeni içerik:

```ts
/**
 * Semantic tokens describe purpose, not raw values.
 * Each brand × mode combination provides values for these slots.
 * Component token groups live in per-component files; this file only
 * composes them into the SemanticTokens contract.
 */
import type { ColorTokens } from "./color";
import type { ButtonTokens } from "./button";
import type { InputTokens } from "./input";
import type { FontTokens, TransitionTokens } from "./common";

export type { ColorTokens, VariantColorTokens } from "./color";
export type { ButtonTokens, ButtonSizeTokens } from "./button";
export type { InputTokens, InputSizeTokens } from "./input";
export type { FontTokens, TransitionTokens } from "./common";

export interface SemanticTokens {
  color: ColorTokens;
  button: ButtonTokens;
  input: InputTokens;
  font: FontTokens;
  transition: TransitionTokens;
}
```

Not: `font`/`transition` alanlarının shape'i değişmiyor (`{ family: {...} }` ve `{ default: string }`) — sadece isimli interface'e taşınıyor. Brand dosyaları (`brand/tulpar/light.ts`, `dark.ts`) `SemanticTokens`'ı `semantic/types`'tan import etmeye devam eder, değişiklik gerekmez.

- [ ] **Step 3: Build + test**

Run: `pnpm --filter @tulpar-ui/tokens run build; pnpm --filter @tulpar-ui/tokens run test`
Expected: build PASS, vitest PASS (CSS çıktısı bire bir aynı — flatten yapısı değişmedi).

Run: `pnpm build`
Expected: tokens'a bağımlı tüm paketler derlenir.

- [ ] **Step 4: Commit**

```bash
git add packages/tokens/src/semantic
git commit -m "refactor(tokens): split semantic types into per-component files"
```

---

## Task 5: Primitive token lint kuralı (component CSS)

**Files:**
- Modify: `eslint.config.js`

- [ ] **Step 1: Kuralı ekle**

`eslint.config.js` dizisine ekle:

```js
  {
    files: ["packages/*/src/**/*.styles.ts"],
    rules: {
      "no-restricted-syntax": [
        "error",
        {
          selector: "TemplateElement[value.raw=/--tulpar-primitive-/]",
          message:
            "Component styles must use semantic tokens (var(--tulpar-...)), never primitives (--tulpar-primitive-...). Bind primitives in the brand layer instead.",
        },
      ],
    },
  },
```

- [ ] **Step 2: Lint çalıştır, ihlalleri değerlendir**

Run: `pnpm lint`

İki olası sonuç:
- PASS → devam.
- `*.styles.ts` dosyalarında ihlal → her biri için: değer semantic token'a taşınabiliyorsa taşı (semantic sözleşmeye eksik slot ekle + brand binding'leri doldur); taşınamıyorsa bu task'ta düzeltme PR'ına dahil et — `eslint-disable` ile susturmak YASAK (kuralın varlık sebebi bu).

Bilinen sınır: `tulpar-button.ts` içindeki `ON_COLOR_MAP` primitive referansı `.styles.ts` dışında olduğu için kural kapsamı dışında — bilinçli; `color` override API'si primitive seçtirme özelliği. Kural genişletilmez.

- [ ] **Step 3: Commit**

```bash
git add eslint.config.js
git commit -m "lint: forbid primitive tokens in component style files"
```

---

## Task 6: PR CI workflow

**Files:**
- Create: `.github/workflows/ci.yml`
- Verify/Modify: `.nvmrc`

- [ ] **Step 1: .nvmrc'yi engines ile hizala**

Run: `Get-Content .nvmrc`
`22.22.3`'ten düşükse (CLAUDE.md hâlâ 20.11.0 diyor) `.nvmrc` içeriğini `22.22.3` yap.

- [ ] **Step 2: Workflow'u yaz**

Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4 # sürümü package.json packageManager'dan okur

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm format:check

      - run: pnpm --filter @tulpar-ui/core run sync-exports:check

      - run: pnpm build

      - name: Install Playwright (core browser tests)
        run: pnpm --filter @tulpar-ui/core exec playwright install --with-deps chromium

      - run: pnpm test
```

- [ ] **Step 3: Yerelde komutları prova et**

Run: `pnpm lint; pnpm format:check; pnpm --filter @tulpar-ui/core run sync-exports:check; pnpm build; pnpm test`
Expected: hepsi PASS (CI'da süpriz kalmasın).

- [ ] **Step 4: Commit + push + PR'da yeşil doğrula**

```bash
git add .github/workflows/ci.yml .nvmrc
git commit -m "ci: add PR verification workflow (lint, format, build, tests)"
```

Push sonrası: GitHub'da workflow'un çalışıp yeşil bittiğini doğrula (`gh run watch` veya Actions sekmesi). Yeşil değilse logs üzerinden düzelt — düzeltme yapılmadan task kapanmaz.

---

## Task 7: Changesets release otomasyonu

**Files:**
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: Release workflow'u yaz**

Create `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    branches: [main]

concurrency: release-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - run: pnpm install --frozen-lockfile

      - name: Create release PR or publish
        uses: changesets/action@v1
        with:
          version: pnpm version-packages
          publish: pnpm release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
```

- [ ] **Step 2: NPM_TOKEN secret'ı (Kaan aksiyonu)**

npmjs.com → Access Tokens → Granular token (publish; `@tulpar-ui` scope) oluştur; GitHub repo Settings → Secrets → Actions → `NPM_TOKEN` olarak ekle. Bu adım Kaan'a aittir — agent secret oluşturamaz; task bu adımda kullanıcıdan onay/bilgi bekler.

- [ ] **Step 3: PR'da changeset hatırlatması (CI'a ek adım)**

`.github/workflows/ci.yml` verify job'ına son adım olarak ekle:

```yaml
      - name: Changeset status (informational)
        run: pnpm exec changeset status --since=origin/main || echo "::warning::No changeset found - add one with 'pnpm changeset' if this PR changes published packages"
```

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/release.yml .github/workflows/ci.yml
git commit -m "ci: changesets release automation + changeset status check"
```

Main'e merge sonrası: Actions'ta Release workflow'un "Version Packages" PR'ı açtığını doğrula.

---

## Task 8: README'ler (root + 4 paket)

**Files:**
- Modify: `README.md` (root)
- Create: `packages/tokens/README.md`, `packages/core/README.md`, `packages/angular/README.md`, `packages/vue/README.md`

- [ ] **Step 1: Root README'yi yeniden yaz**

`README.md` içeriği (canlı site linkleri memory'deki deploy hedefleri):

```markdown
# Tulpar UI

From-scratch design system: Lit-based Web Components with idiomatic
Angular and Vue wrappers. Single source of truth for behavior and
rendering, consumable from any framework.

**Docs / Demos:** [Storybook](https://tulpar-ui-docs.pages.dev) ·
[Angular playground](https://tulpar-ui-ng.pages.dev) ·
[Vue playground](https://tulpar-ui-vue.pages.dev)

## Packages

| Package | Description |
| --- | --- |
| `@tulpar-ui/tokens` | 3-layer design tokens (primitive → semantic → brand×mode), generated CSS custom properties |
| `@tulpar-ui/core` | Lit Web Components (`<tulpar-button>`, input family) — source of truth |
| `@tulpar-ui/angular` | Signal-based Angular wrappers (`<tulpar-button-ng>`) |
| `@tulpar-ui/vue` | Vue 3 SFC wrappers (`<TulparButton>`) |

## Quick start (Vue)

```bash
pnpm add @tulpar-ui/vue @tulpar-ui/tokens
```

```ts
// main.ts
import "@tulpar-ui/tokens/css/tulpar.css";
```

```vue
<script setup>
import { TulparButton } from "@tulpar-ui/vue";
</script>
<template>
  <TulparButton severity="primary">Save</TulparButton>
</template>
```

Vite config'te custom element bildirimi gerekir:

```ts
vue({ template: { compilerOptions: { isCustomElement: (tag) => tag.startsWith("tulpar-") } } })
```

## Quick start (Angular)

```bash
pnpm add @tulpar-ui/angular @tulpar-ui/tokens
```

```ts
// styles üzerinden: angular.json styles dizisine
// "node_modules/@tulpar-ui/tokens/dist/css/tulpar.css"
import { TulparButtonComponent } from "@tulpar-ui/angular";
```

## Theming

- Brand: `<html data-brand="tulpar">` (default `:root`)
- Dark mode: `.dark` class (Tailwind-compatible)
- Override: any `--tulpar-*` custom property

## Development

```bash
pnpm install
pnpm build          # all packages, topological
pnpm test           # all tests
pnpm dev            # watch everything + playgrounds + Storybook
```

## License

See [LICENSE](./LICENSE).
```

- [ ] **Step 2: Paket README'leri**

Her paket için kısa README (aynı şablon; paket adı/kurulum/iki satır kullanım + root'a link). Örnek `packages/core/README.md`:

```markdown
# @tulpar-ui/core

Lit-based Web Components for Tulpar UI — the source of truth for
behavior and rendering. Framework wrappers: `@tulpar-ui/angular`,
`@tulpar-ui/vue`.

```bash
pnpm add @tulpar-ui/core @tulpar-ui/tokens
```

```ts
import "@tulpar-ui/tokens/css/tulpar.css"; // once, app entry
import "@tulpar-ui/core/button";
```

```html
<tulpar-button severity="primary">Save</tulpar-button>
```

Full docs: https://github.com/kaanakcan/tulpar-ui#readme
```

Diğer üçü aynı kalıpta: tokens (CSS import + katman açıklaması 3 satır), angular (`TulparButtonComponent` + `tulpar-button-ng` selector notu), vue (`TulparButton` + isCustomElement notu). GitHub URL'ini gerçek repo remote'undan doğrula: `git remote get-url origin`.

- [ ] **Step 3: Doğrula + commit**

Run: `pnpm format:check` (yeni md'ler prettier uyumlu olmalı; değilse `pnpm format`)

```bash
git add README.md packages/*/README.md
git commit -m "docs: root README overhaul + per-package READMEs"
```

---

## Task 9: LICENSE kararı (Kaan gate'i)

**Files:**
- Create: `LICENSE`
- Modify: `package.json` (root) + `packages/*/package.json` (`license` alanı)

- [ ] **Step 1: Kaan'a seçenekleri sun (AskUserQuestion)**

Bu adım kullanıcı kararı gerektirir, agent kendi karar VEREMEZ. Seçenekler:

1. **MIT** — maksimum adoption; ticarileşme component-library'lerde genelde "open core + pro paket" modeliyle yine mümkün (PrimeVue/PrimeNG modeli). Önerilen.
2. **Apache-2.0** — MIT + patent grant; kurumsal tüketiciler için bir tık daha güvenli.
3. **Proprietary / All rights reserved** — npm'de yayın anlamını yitirir (kullanım sözleşmesi gerekir); adoption sıfıra yakın.

- [ ] **Step 2: Seçilen lisans dosyasını ekle**

`LICENSE` dosyası: seçilen lisansın standart tam metni, copyright satırı `Copyright (c) 2026 Kaan Akcan`.

Tüm `packages/*/package.json` dosyalarına `"license": "<SPDX>"` alanı ekle (örn. `"license": "MIT"`); root package.json'a da.

- [ ] **Step 3: Commit**

```bash
git add LICENSE package.json packages/*/package.json
git commit -m "docs: add LICENSE and license fields"
```

---

## Task 10: CLAUDE.md güncellemesi

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Güncellenecek bölümler**

Mevcut CLAUDE.md'de şu düzeltmeler yapılır (kalan içerik korunur):

1. **Status satırı:** `**Status:** v0.5 (input family) complete; v0.6 in progress — shell family (@tulpar-ui/shell) + infra hardening.`
2. **Authoritative Docs:** listeye ekle:
   - `docs/superpowers/specs/2026-06-10-tulpar-ui-v0.6-shell-design.md`
   - `docs/superpowers/plans/2026-06-10-tulpar-ui-v0.6a-infra-quality.md`
   - `docs/superpowers/plans/2026-06-10-tulpar-ui-v0.6b-shell-family.md`
   - `docs/architecture/internal-layering.md`
3. **Architecture bloğuna satır ekle:** `@tulpar-ui/shell → App shell (tulpar-shell, tulpar-topbar, tulpar-sidenav, tulpar-nav-item)`
4. **Tech stack tablosu:** Node satırını `.nvmrc` gerçeğiyle eşitle (22.22.3); pnpm satırına `pnpm 9 + catalogs` ekle.
5. **Components durumu:** "Single component (Button...)" cümlesini güncelle: Button/ButtonGroup + TextInput/NumberInput/Textarea mevcut.
6. **Out of Scope listesi:** v0.6 gerçeğine çek — Input artık kapsamda DEĞİL-dışı (yapıldı); shell v0.6'da; "Configurator, runtime renk preset, top/mix layout, breadcrumb, RTL v0.6 dışı" satırları eklenir.
7. **Working Conventions'a ekle:** `_internal` kuralı için `docs/architecture/internal-layering.md` referansı; primitive-token lint kuralı notu; tasarım kararlarında `/frontend-design` + `/ui-ux-pro-max` zorunluluğu.

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: refresh CLAUDE.md for v0.6 reality"
```

---

## Tamamlanma kriteri

- [ ] Task 1–5 bitti → v0.6b (shell) planı başlayabilir
- [ ] CI PR'larda zorunlu yeşil; release workflow main'de Version PR üretiyor
- [ ] `pnpm lint && pnpm format:check && pnpm build && pnpm test` lokalde tek seferde yeşil
